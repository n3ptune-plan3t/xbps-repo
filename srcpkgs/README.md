# srcpkgs

This directory is intentionally empty so you can add your own XBPS package templates.

Add one directory per package, for example:

- `srcpkgs/my-package/template`
- `srcpkgs/another-package/template`

After you add templates and push changes, GitHub Actions will build and publish them.
